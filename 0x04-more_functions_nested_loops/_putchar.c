#include "main.h"
#include <unistd.h>

/**
 * _isupper - write letter c to stdout
 * @c: the letter to print
 *
 * Return: on success 1.
 * on error, -1 is returned, and errno is set
 * 
 */

int _putchar(char c);
{
	return (write(1, &C, 1));
}
